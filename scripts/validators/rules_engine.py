# Placeholder per estendere regole semantiche: espressioni, overlay, topologia.
# TODO: implementare parser 'when/assert' e funzioni geometriche (overlaps, area_m2) con CRS metrico.

def eval_rules(gdf, rules):
    return []